-use yii\helpers\Html
-use yii\helpers\Url
-use yii\widgets\ListView
-$view->title=Yii::t('app','Projects')
-$view->beginBlock('content-header')
|#{$view->title} <small>all your bugs are belong to us</small>
-$view->endBlock()

if (\Yii::$app->user->can('project_create'))
  p
    !=Html::a(Yii::t('app', 'Create Project'), ['create'], ['class' => 'btn btn-success pull-right'])

hr
!=ListView::widget(['dataProvider' => $dataProvider,'itemOptions' => ['class' => 'item'],'itemView' => 'partials/_item'])
